# 餐饮门店端（admin）接入说明（基于 yudao-ui-admin-vue3）

本目录 `yudao-ui-admin-vue3` 是芋道官方管理后台（**Vue3 + Element Plus**）。餐饮门店端按框架约定接入，复用其 `axios` 封装、字典、表格、分页等公共能力。

## 1. 关键约定（与 mall-uniapp 不同）

- **菜单/路由是后端驱动**：登录后 `setUserInfoAction` 拉取菜单，`permission.generateRoutes()` 调 `generateRoute(res)` 把 `component` 字段映射成 `() => import('@/views/...')`。因此**前端页面写完不会自动出现**，必须后端 `system_menu` 里有对应菜单记录（见 `m6_menu.sql`）。
- **API 封装**：`src/api/<模块>/*.ts` 内 `import request from '@/config/axios'`，方法 `request.get/post/put/delete({ url, params?, data? })`。
- **页面布局**：列表页用 `<ContentWrap>` + `<el-form>` 搜索 + `<el-table>` + `<Pagination>`，公共组件在 `src/components`。
- **前缀**：门店端走 `/admin-api`，且用户为 **ADMIN** 账号（与 `/admin-api` 的 userType 校验匹配）。门店登录走微信小程序 `POST /admin-api/store-auth/weixin-mini-app-login`（无独立后台登录页，店员用小程序扫码/账号登录后在后台通过角色授权访问）。
- **金额单位**：分（Integer），页面 `price / 100` 转元。

## 2. 新增文件清单

### API：`src/api/restaurant/index.ts`
聚合所有门店端接口（按真实后端路由）：
- 门店：`/store/info/{page,get,simple-list,create,update,delete}`
- 桌台：`/store/table/{page,simple-list,create,update,delete,generate}`
- 菜品：`/store/dish/{page,get,create,update,delete}`
- 订单：`/store/order/{page,get,create,add-items,cancel,accept,complete}`
- 会员：`/store/member/{page,get}`
- 优惠券模板：`/store/coupon-template/{page,create,update,delete}`
- 套餐（平台级）：`/platform/package/{page,create,update}`、`/platform/subscription/{open,page}`

### 页面：`src/views/restaurant/*`（已在 `m6_menu.sql` 注册 component 路径）
| 页面 | component | 菜单权限 | 说明 |
|---|---|---|---|
| `store/index.vue` | `views/restaurant/store/index` | `restaurant:store:query` | 门店列表/增删 |
| `table/index.vue` | `views/restaurant/table/index` | `restaurant:table:query` | 桌台列表/批量生成 |
| `dish/index.vue` | `views/restaurant/dish/index` | `restaurant:dish:query` | 菜品列表 |
| `order/index.vue` | `views/restaurant/order/index` | `restaurant:order:query` | 订单列表 + 接单/完成/取消 |
| `member/index.vue` | `views/restaurant/member/index` | `restaurant:member:query` | 会员档案 |
| `coupon/index.vue` | `views/restaurant/coupon/index` | `restaurant:coupon:query` | 券模板 |
| `package/index.vue` | `views/restaurant/package/index` | `restaurant:package:query` | 套餐与订阅（平台级） |

> 页面目前是**骨架级列表页**（含查询、分页、状态标签、操作按钮占位）。新建/编辑表单（`form/index.vue`）、订单详情（`detail/index.vue`）按需补充——按钮已留 `openCreate/openEdit` 空函数便于扩展。

### 菜单种子：`M0-code/sql/m6_menu.sql`
- 父目录「餐饮管理」`/restaurant`（id=10000），下挂 7 个子菜单（id 10010~10070）。
- component 字段指向对应 `views/restaurant/*/index`，权限标识用 `@ss:` 风格（`restaurant:*:query`）。
- 已授权给管理员角色（role_id=1），执行后后台左侧即出现「餐饮管理」菜单。
- 幂等：先 `DELETE` 区间再 `INSERT`。

## 3. 联调步骤

1. 后端 `m6_menu.sql` 执行（或后台【系统管理-菜单】手动加）。
2. 【系统管理-角色】给门店店员角色勾选餐饮菜单（权限标识 `restaurant:*:query` 等）。
3. 店员用 admin 账号登录后台，左侧出现「餐饮管理」。
4. 门店/桌台/菜品/订单/会员/优惠券/套餐各页可正常 CRUD（接口走 `/admin-api/store/*`、`/admin-api/platform/*`）。

## 4. 与消费者端的区别
| 端 | 工程 | 前缀 | 账号体系 | 菜单来源 |
|---|---|---|---|---|
| 消费者 | yudao-mall-uniapp | `/app-api` | 会员 MEMBER | 前端静态路由 |
| 门店/后台 | yudao-ui-admin-vue3 | `/admin-api` | 管理员 ADMIN | 后端 `system_menu` 驱动 |

门店登录的特殊点：店员身份是 admin 账号，但首次绑定走微信小程序的 `/store-auth/weixin-mini-app-login`（返回 admin token），登录态与后台共用。
